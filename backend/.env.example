# Optional. If unset, POST /api/ask automatically falls back to a
# deterministic rule-based answer engine so the app still works end-to-end
# without an API key.
ANTHROPIC_API_KEY=your_key_here

# Optional. Defaults to a small, fast Claude model.
LLM_MODEL=claude-haiku-4-5-20251001
